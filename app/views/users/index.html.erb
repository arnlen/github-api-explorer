<h1>Known users</h1>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to(user.username, "/users/#{user.username}") %> (<%= pluralize(user.repository_counter, "repository") %>)
    </li>
  <% end %>
</ul>